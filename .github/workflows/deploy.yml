name: Deploy to FTP server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: ftp-action
      uses: sebastianpopp/ftp-action@v2.0.0
      with:
        host: wpdemo2.com
        user: wpdemo2c
        password: 612m171c83DX_3p
        remoteDir: public_html/new
